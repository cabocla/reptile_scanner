import 'package:flutter/material.dart';

class IncubatorListPage extends StatelessWidget {
  static const routeName = '/incubator-list';
  const IncubatorListPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Container(),
    );
  }
}
