import 'package:flutter/material.dart';

class BreedingListPage extends StatelessWidget {
  static const routeName = '/breeding-list';
  const BreedingListPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Container(),
    );
  }
}
