import 'package:flutter/material.dart';

class ColonyActivityForm extends StatelessWidget {
  const ColonyActivityForm({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
