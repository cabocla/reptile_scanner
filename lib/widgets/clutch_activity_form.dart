import 'package:flutter/material.dart';

class ClutchActivityForm extends StatelessWidget {
  const ClutchActivityForm({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
