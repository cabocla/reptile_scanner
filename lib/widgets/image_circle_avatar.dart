import 'package:flutter/material.dart';

class ImageCircleAvatar extends StatelessWidget {
  final String imagePath;
  const ImageCircleAvatar(this.imagePath, {Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
