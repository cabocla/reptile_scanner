import 'package:flutter/material.dart';

class FeederActivityForm extends StatelessWidget {
  const FeederActivityForm({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
